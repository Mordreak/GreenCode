<style>
    body {
        height: auto !important;
    }

    .detail.container {
        margin: 80px auto 0;
        padding: 2rem;
        {% if app.request.get('_theme') == "bright" %}
            background: white;
        {% else %}
            background: #4C4C4C;
            color: white!important;
        {% endif %}
        display: flex;
        flex-direction: column;
        max-width: 480px;
        font-family: Arial, sans-serif;
        box-shadow:2px 2px 2px gray;
        border-radius: 6px;
    }

    .value a {
        {% if app.request.get('_theme') != "bright" %}
            color: grey;
        {% endif %}
    }

    .name {
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1rem;
    }

    .name img {
        width: auto;
        height: auto;
        margin-right: 1rem;
    }

    .item {
        display: flex;
        justify-content: center;
    }

    .label {
        margin-right: 0.5rem;
        text-align: right;
    }

    .value {
        margin-left: 0.5rem;
        text-align: left;
    }

    .opened {
        text-align: center;
    }

    .contact {
        margin-top: 1rem;
    }

    .contact h4 {
        font-weight: 500;
    }

    .opening-hours {
        display: flex;
        flex-direction: column;
        justify-content: center;
        margin-top: 1rem;
    }

    .opening-hours .label {
        width: 50%;
    }

    .opening-hours .value {
        width: 50%;
    }

    .opening-hours .opened {
        margin-bottom: 1rem;
    }

    .opening-hours .item {
        font-size: 14px;
    }

    .opening-hours h5 {
        text-align: center;
        text-decoration: underline;
        margin-bottom: 5px;
    }
</style>