<style>

    /* Homepage */
    .homepage .wrapper {
        padding-top: 100px;
    }
    .homepage h1 {
        margin: 0;
        font-size: 26px;
        line-height: 30px;
        font-family: Arial, sans-serif;
        font-weight: 700;
        text-align: center;
    }

    .homepage h4 {
        margin: 16px 0px 25px;
        font-size: 16px;
        font-family: Arial, sans-serif;
        font-weight: 400;
        text-align: center;
    }

    .homepage.container {
        width:100%;
        height: 100vh;
        margin:auto;
        position: relative;
        background-image: url({{ asset('assets/background.png') }});
        background-repeat: no-repeat;
        background-position-x: center;
        background-position-y: bottom;
    }

    /* Search */

    .search-box {
        display: flex;
        justify-content: center;
        position: relative;
        top:50px;
        height:50px;
        transition:opacity 0.4s linear ,visibility 0.4s linear 0s;
    }

    .search-box > #search {
        width:50%;
        height:50px;
        box-shadow: 1px 2px 2px #ddd;
        border-radius: 6px 0px 0px 6px;
    }
    .search-box > #select {
        width:20%;
        height:50px;
        border:none;
        box-shadow: 1px 2px 2px #ddd;
    }
    .homepage select {
        border:none;
        box-shadow:none;
        background-color:none;
        cursor: pointer;
        outline: 0;
        font-size:16px;
        font-family: Arial, sans-serif;
        color:#bbb;
        padding:0px 12px;
        -webkit-box-sizing: border-box;
        -moz-box-sizing: border-box;
        box-sizing: border-box;
        -webkit-appearance: none;
        -moz-appearance: none;
    }
    .homepage input ,.homepage input:focus {
        border:none;
        box-shadow:none;
        background-color: none;
        outline: 0;
        font-size:16px;
        font-family: Arial, sans-serif;
        color:#bbb;
        padding:0px 12px;
    }
    .homepage .also {
        color:#bbb;
        background-color:#fff;
        height:50px;
        width:50px;
        border:none;
        line-height: 50px;
        text-align: center;
        cursor: pointer;
        top:0;
        box-shadow: 1px 2px 2px #ddd;
        border-radius:0px 6px 6px 0px;

    }
    .homepage .search-link:hover {color:#686868;}
    .homepage select.minimal {
        background-image: linear-gradient(45deg, transparent 50%, gray 50%), linear-gradient(135deg, gray 50%, transparent 50%);
        background-position: calc(100% - 15px) calc(1em + 5px), calc(100% - 10px) calc(1em + 5px);
        background-size: 5px 5px, 5px 5px;
        background-repeat: no-repeat;
    }

    .homepage .search-button {
        height: 30px;
        padding: 10px;
        fill: grey;
    }
</style>
